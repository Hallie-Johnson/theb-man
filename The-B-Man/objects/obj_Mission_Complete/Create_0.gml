/// @description Insert description here
// You can write your code in this editor

/*
image_alpha = 0;

// Create Event
scale_start = 0.1;    // Starting scale
scale_end = 1;        // End scale (normal size)
rotation_speed = 16;   // Speed of rotation in degrees per step
scaling_duration = 90; // Duration of scaling and rotation (frames)
rotation_target = 0;  // Target rotation angle

// Initialize
current_scale = scale_start;
current_rotation = 0;
scale_increment = (scale_end - scale_start) / scaling_duration;
rotation_increment = rotation_speed;
scaling_timer = 0;
*/