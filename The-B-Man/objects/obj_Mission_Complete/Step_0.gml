/// @description Insert description here
// You can write your code in this editor

/*
// Step Event

// Increment scaling and rotation
if (scaling_timer < scaling_duration) {
    // Update scale
    current_scale += scale_increment;
    if (current_scale > scale_end) {
        current_scale = scale_end;
    }

    // Update rotation
    current_rotation += rotation_increment;
    if (current_rotation >= 360) {
        current_rotation -= 360; // Keep rotation within 0-359 degrees
    }

    // Apply transformations
    image_xscale = current_scale;
    image_yscale = current_scale;
    image_angle = current_rotation;

    // Increment the timer
    scaling_timer++;
	
} else {
    // Ensure the object ends with normal size and 0 rotation
    image_xscale = scale_end;
    image_yscale = scale_end;
    image_angle = rotation_target;
	//room_goto(rm_IconHome);
}

y = obj_Battle_Bahaman.y

image_alpha = image_alpha + 0.01;
*/